Options -Indexes
DirectoryIndex index.php

<IfModule mod_rewrite.c>
    RewriteEngine On
</IfModule>

<FilesMatch "^\.">
    Require all denied
</FilesMatch>

RewriteRule ^(includes|sql|scripts|vendor|storage|cache|logs|tmp)(/|$) - [F,L,NC]
